{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --esm --transpile-only src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}